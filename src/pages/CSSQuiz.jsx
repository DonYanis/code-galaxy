import React from 'react'
import { Quiz } from '../components'
import { cssData } from '../data'

const CSSQuiz = () => {
  return (
    <Quiz name={'CSS'} data={cssData}/>
  )
}

export default CSSQuiz